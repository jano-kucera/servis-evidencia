/**
 * Order item.
 */
export interface Item {
    /** Item's description. */
    description: string;

    /** Item's description. */
    name: string;

    /** Item's price. */
    price: number;

    /** Item's quantity. */
    quantity: number;
}
